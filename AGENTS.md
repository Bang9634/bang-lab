# 저장소 작업 지침

## 소통과 문서 언어

- 근거가 약한 가정은 구체적인 논리와 증거로 검토하고 반박한다.
- 불필요한 질문이나 후속 작업 제안으로 답변을 끝내지 않는다.
- README, 개발 문서, AI 컨텍스트 문서와 방문자에게 보이는 콘텐츠는 한글로 작성한다.
- 코드 식별자, 코드 주석과 커밋 메시지는 영어를 기본으로 한다.
- 기술 용어, 명령어, 파일 경로는 의미가 명확하도록 원문을 유지한다.
- 사용자가 별도로 지정한 언어가 있으면 그 요청을 따른다.

## 프로젝트 맥락

본격적인 변경 전에 `docs/ai-context/`의 `project-overview.md`,
`coding-rules.md`, `current-state.md`를 읽는다.
구현 또는 검증 상태가 달라지면 현재 상태 문서도 갱신한다.

## 구현 원칙

- Astro 정적 생성과 Markdown/MDX 콘텐츠 컬렉션을 우선한다.
- React가 필요한 상호작용에만 React island를 추가한다.
- 실제 서버 상태 요구사항이 생길 때만 백엔드를 추가한다.
  Java/Spring Boot/Maven은 향후 후보이며 현재 의존성이 아니다.
- `apps/web`, 향후 필요 시 `apps/api`, `infra`, `docs`의 모노레포 구조를 유지한다.
- 추측에 기반한 추상화와 불필요한 의존성을 피한다.
- 비밀정보를 커밋하지 않는다. 측정 결과나 개인 경험을 만들어내지 않는다.
- 초안 글은 공개 목록과 생성되는 상세 페이지에서 제외한다.
- 소스 코드는 MIT, 블로그 글과 직접 작성한 콘텐츠는 All rights reserved를 적용한다.
- 영어 Conventional Commit 형식(`feat:`, `fix:`, `docs:` 등)을 선호한다.

## 검증

관련 사이트 변경에는 `npm run check`와 `npm run build`를 실행한다.
초안 제외, 콘텐츠 렌더링 등 변경한 동작을 중심으로 검증한다.
실행하지 못한 검증은 기록하고, 로컬 빌드 성공을 배포 성공으로 표현하지 않는다.
